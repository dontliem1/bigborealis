{% extends 'layouts/base.njk' %}

{% block content %}
  <main>
    <h1 class='page__title'>{{ title }}</h1>
    <div>
      {{ content | safe }}
    </div>
  </main>
{% endblock %}

{{ content | safe }}
